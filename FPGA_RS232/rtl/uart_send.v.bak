//************************************************************************
// uart_send为串口发送模块，以uart_done
// 为发送使能信号，将接收到的数据uart_data通过串口发送端口uart_txd发送出去。
//************************************************************************

module uart_send(
	
	input clk,
	input rst_n,
	
	output uart_txd,
	input reg[7:0]data,
	input uart_done
	
	);


endmodule	